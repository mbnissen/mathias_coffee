name: Test & Deploy

on:
  push:
    branches:
      - "main"

jobs:
  test:
    name: Test
    uses: ./.github/workflows/test.yaml
    secrets: inherit # This is required for child workflows to use secrets

  deploy:
    name: Deploy
    uses: ./.github/workflows/test.yaml
    secrets: inherit # This is required for child workflows to use secrets
